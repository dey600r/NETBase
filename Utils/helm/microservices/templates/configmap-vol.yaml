{{- if .Values.configmapVolume.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: cm-vol-{{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
data:
{{- range $key, $value := .Values.configmapVolume.data }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
{{- end }}