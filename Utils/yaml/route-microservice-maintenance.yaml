apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: route-microservice-maintenance
  namespace: microservices
  labels:
    app: microservices
    app.kubernetes.io/part-of: microservices
spec:
  host: microservice-maintenance-api.apps-crc.testing
  to:
    kind: Service
    name: svc-microservices
  port:
    targetPort: maintenance-tcp
  tls:
    termination: edge
    wildcardPolicy: None