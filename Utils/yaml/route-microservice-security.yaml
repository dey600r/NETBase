apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: route-microservice-security
  namespace: microservices
  labels:
    app: microservices
    app.kubernetes.io/part-of: microservices
spec:
  host: microservice-security-api.apps-crc.testing
  to:
    kind: Service
    name: svc-microservices
  port:
    targetPort: security-tcp
  tls:
    termination: edge
    wildcardPolicy: None